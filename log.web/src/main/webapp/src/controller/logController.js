define(['controller/_logController','delegate/logDelegate'], function() {
    App.Controller.LogController = App.Controller._LogController.extend({

    });
    return App.Controller.LogController;
}); 